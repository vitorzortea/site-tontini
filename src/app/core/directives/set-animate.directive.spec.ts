import { SetAnimateDirective } from './set-animate.directive';

describe('SetAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new SetAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
